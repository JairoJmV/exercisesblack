// ex: const = 3 imprimir */**//***///
const caracter = [3];
const mul = '*';

for (let index = 1; index < caracter; index++) {
    if (caracter[index] === caracter) {
        mul++
    }
};
console.log(mul);